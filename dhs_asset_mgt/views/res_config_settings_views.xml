<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.asset.form.inherit</field>
        <field name="model">res.config.settings</field>
        <field name="priority" eval="71"/>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="IT Asset" string="IT Asset" name="dhs_asset_mgt" groups="dhs_asset_mgt.group_it_asset_admin">
                    <block title="Employees" name="employees_setting_container">
                        <setting id="admins_notification_ids" title="Admins Notification" help="If more than one asset is registered for someone, an email will be sent to the manager">
                            <field name="admins_notification_ids">
                                <tree>
                                    <field name="name" />
                                    <field name="email" />
                                </tree>
                            </field>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>

    <record id="asset_config_settings_action" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context">{'module' : 'dhs_asset_mgt', 'bin_size': False}</field>
    </record>


</odoo>
