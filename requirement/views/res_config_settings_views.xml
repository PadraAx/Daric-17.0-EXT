<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="res_config_settings_view_form" model="ir.ui.view">
            <field name="name">res.config.settings.view.form.inherit.requirement</field>
            <field name="model">res.config.settings</field>
            <field name="priority" eval="90"/>
            <field name="inherit_id" ref="base.res_config_settings_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="//form" position="inside">
                            <app data-string="Requirement" string="Requirement" name="requirement" groups="requirement.group_bra_admin">
                        <block title="Based on Reviewer Points" name="requirement_point_based_setting_container">
                            <setting title="Select how you want to calculate the average review score: Based on all reviews or reviewer points." help="Select 'Based on Reviewer Points' to calculate the review score"
                            documentation="">
                                <field name="module_requirement_point_based"/>
                            </setting>
                        </block>
                        <div class="alert alert-info">
                            <p>Select how you want to calculate the average review score: Based on all reviews or reviewer points.</p>
                        </div>
                        </app>
                        </xpath>
                   
                </field>
        </record>

        <record id="requirement_config_settings_action" model="ir.actions.act_window">
            <field name="name">Settings</field>
            <field name="res_model">res.config.settings</field>
            <field name="view_mode">form</field>
            <field name="target">inline</field>
            <field name="context">{'module' : 'requirement', 'bin_size': False}</field>
        </record>

        <menuitem id="requirement_config_settings_menu" 
            name="Settings"
            parent="menu_configuration" 
            sequence="0" 
            action="requirement_config_settings_action"
            groups="base.group_system"/>
</data>
</odoo>
